html
	head
		title title
	body
		include ./header.pug
		
		//interpolation
		p.greetings#people Hello  from #{name}!		
		h1= message
		ul
			each val in products
				li= val
				
		div.container.column.main#division(width="100", height="100")
			| To insert block of text,
			| You can use the pipe opeartor
			
		p Pug conditionals
		if(name)
			h2 Hi, #{name}
		else
			a(href = "/login") LogIn
			
		include ./footer.pug